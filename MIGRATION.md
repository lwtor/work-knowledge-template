# Personal Knowledge Base Layout Migration

本协议只在用户明确要求“迁移知识库目录结构”时生效。框架更新、状态检查、初始化、查询和写入都不构成迁移授权。

## 核心边界

1. 框架更新只安装双布局能力和迁移工具，不移动个人数据。
2. 没有 .kb-layout-version 的私人库按布局 1 正常使用；不得视为错误。
3. 用户拒绝迁移时继续完整使用布局 1，不得反复提示。
4. 迁移前必须先运行预览并明确告诉用户当前可正常使用、迁移是可选操作。
5. 只有用户确认预览后才能执行；Git 提交和推送仍需分别确认。

## 必须提示

预览完成后必须给出可直接回复的下一步：

“目录迁移预览已经生成，当前旧目录仍可正常使用。若确认按以上范围迁移，请回复：确认执行目录迁移，不提交、不推送。”

迁移成功后必须提示新的 Obsidian 打开路径，并询问：

“目录迁移已完成，但 Git 尚未记录这些变化。是否要我先展示迁移差异并创建本地 Git 提交？不会自动推送。”

## 执行要求

- 目标必须是 personal Git 仓库且工作区干净。
- 只允许从布局 1 迁移到布局 2。
- 创建完整备份和迁移状态文件。
- 目标路径冲突、未知布局或未完成迁移必须停止，不得覆盖。
- 先机械映射旧目录，不自动重新分类个人笔记。
- 不认识的用户文件保留原位并报告。
- 验证迁移前后文件数量、链接、索引和附件。
- 执行完成前不得把 .kb-layout-version 改为 2。

Windows 使用 scripts/migrate-layout.ps1，macOS/Linux 使用 scripts/migrate-layout.sh。两者只包装 scripts/kb-migrate-layout.py，不得自行实现另一套迁移逻辑。
