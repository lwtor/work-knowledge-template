# Migrate the content layout

Only use this route when the user explicitly asks to migrate the directory structure. Read the repository root MIGRATION.md completely. First run preview only. State that layout 1 remains usable and end with the required-confirmation block and exact reply from the protocol. Never treat framework-update approval as migration approval. Commit and push require separate authorization.
